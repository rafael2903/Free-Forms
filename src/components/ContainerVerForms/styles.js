import styled from 'styled-components';
import Main from '../Main';

export const ContainerVerForms = styled(Main)`
  padding: 1.5rem;

  .ButtonsContainer {
    display: flex;
    height: 35px;
  }
`;
